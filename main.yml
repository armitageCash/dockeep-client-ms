openapi: "3.0.1"
info:
  title: Doockeep clients microservice
  version: "1.0"
servers:
  - url: "http://dockeep-clients-ms-elb-690144778.us-east-2.elb.amazonaws.com/"
paths:
  /apigateway:
    get:
      responses:
        "200":
          description: Respuesta exitosa
      x-amazon-apigateway-integration:
        type: http_proxy
        uri: "/apigateway" # Esto debería ser un URI relativo a la URL del servidor
        httpMethod: GET
