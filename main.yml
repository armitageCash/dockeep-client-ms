openapi: "3.0.1"
info:
  title: Doockeep clients microservice
  version: "1.0"
paths:
  /apigateway:
    get:
      responses:
        "200":
          description: Respuesta exitosa
      x-amazon-apigateway-integration:
        type: http_proxy
        uri: "http://${stageVariables.ECS_ELB_ENDPOINT}/apigateway" # Reemplaza con la URL real de tu ELB
        httpMethod: GET
        connectionType: VPC_LINK # Si tu servicio ECS está en la misma VPC que la API Gateway
        connectionId: eice-0dcee94236cf7f81e # Reemplaza con el ID de tu VPC Link
